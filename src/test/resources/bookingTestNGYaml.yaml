name: YAML Test suite
parallel: classes
threadCount: 2
tests:
  - name: Booking tests
    classes:
      - tests.booking.BookingAddToFavorites
      - tests.booking.BookingHeaderElements
      - tests.booking.BookingNewUser